import {SafeAreaView, ScrollView, Text} from 'react-native';
import React from 'react';

export const App = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Hello, World!</Text>
      </ScrollView>
    </SafeAreaView>
  );
};
